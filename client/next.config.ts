import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: ['svc-f49cab94-morphvm-gomj7a0m.http.cloud.morph.so'],
};

export default nextConfig;
